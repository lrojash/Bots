// created a seperate class if we want to use different shape objects. but they all carry the same attributes. 
class Robots {
    constructor(context, x, y, vx, vy) {
        this.context = context
        this.x = x 
        this.y = y 
        this.vx = vx 
        this.vy = vy
    }
}